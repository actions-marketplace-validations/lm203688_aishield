{
  "serverInfo": {
    "name": "AIShield",
    "version": "4.8.3",
    "description": "AI Agent Security Shield — OWASP MCP Top 10 + OWASP Agentic AI Top 10 aligned security scanning. 235 MCP rules (+262 skill rules) covering prompt injection, zero-width characters, Rug Pull, permission audit, supply-chain and agentic risk. Local-first: no code leaves your machine. Features pre-install guardrail gating, OpenAPI auto-discovery, compact trust digest (aishield-digest/v1) and embeddable security badges."
  },
  "url": "https://github.com/lm203688/aishield",
  "transport": "stdio",
  "install": "npx -y aishield-mcp-server",
  "provider": {
    "name": "AIShield",
    "url": "https://github.com/lm203688/aishield"
  },
  "license": "MIT",
  "tools": [
    {
      "name": "aishield_scan",
      "description": "Full security scan of an MCP server / AI skill / agent (GitHub URL). Returns 5-dimension score + OWASP coverage + detailed findings.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "source_url": {
            "type": "string",
            "description": "GitHub or repo URL of the MCP server / skill / agent to scan"
          }
        },
        "required": [
          "source_url"
        ]
      }
    },
    {
      "name": "aishield_guardrail",
      "description": "Pre-install gate: pass/block verdict before installing any third-party MCP server.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "source_url": {
            "type": "string",
            "description": "GitHub or repo URL of the MCP server to evaluate before install"
          }
        },
        "required": [
          "source_url"
        ]
      }
    },
    {
      "name": "aishield_prompt_check",
      "description": "Detect prompt injection / jailbreak / data-exfiltration in user prompts (zh + en).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "description": "Text to check for prompt injection"
          }
        },
        "required": [
          "prompt"
        ]
      }
    },
    {
      "name": "aishield_banned_words",
      "description": "Chinese banned-word detection across 6 platforms: WeChat, Douyin, Xiaohongshu, Bilibili, Weibo, Kuaishou.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string",
            "description": "Text to check"
          },
          "platform": {
            "type": "string",
            "enum": [
              "all",
              "wechat",
              "douyin",
              "xiaohongshu",
              "bilibili",
              "weibo",
              "kuaishou"
            ],
            "default": "all"
          }
        },
        "required": [
          "text"
        ]
      }
    },
    {
      "name": "aishield_rug_pull",
      "description": "Detect rug-pull: security-code removal or suspicious additions across recent commits.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "source_url": {
            "type": "string",
            "description": "GitHub repository URL"
          }
        },
        "required": [
          "source_url"
        ]
      }
    },
    {
      "name": "aishield_handshake",
      "description": "MCP handshake verification: npx auto-install risk, sensitive env vars, oversized tool descriptions.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "source_url": {
            "type": "string",
            "description": "GitHub or package URL to verify"
          }
        },
        "required": [
          "source_url"
        ]
      }
    },
    {
      "name": "aishield_digest",
      "description": "Compact trust digest (aishield-digest/v1): ~520 bytes + a content fingerprint (sha256). Lets an agent answer 'can I trust this config?' every turn without re-pulling the full report. Accepts {configs} (static analysis only, no spawn), {scan_result}, or {source_url}. The risk label is floored by the worst finding — it will never say 'safe' for a config that has a high/critical finding.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "configs": {
            "type": "string",
            "description": "MCP client config (JSON text) to analyze statically — no commands are spawned"
          },
          "scan_result": {
            "type": "object",
            "description": "A previous AIShield scan result to compress into a digest"
          },
          "source_url": {
            "type": "string",
            "description": "Remote URL of an MCP server to evaluate"
          }
        }
      }
    }
  ],
  "trust": {
    "standard": "OWASP MCP Top 10 (2025 v0.1) + OWASP Agentic AI Top 10",
    "local_first": true,
    "open_source": "MIT",
    "verification": "self-attested, open-source — no third-party audit claimed"
  },
  "category_id_note": "The owasp_category values ASI01-ASI10 are AIShield's INTERNAL category IDs, not the official OWASP Top 10 for Agentic Applications (2026) identifiers. Only ASI01/ASI02/ASI03 coincide. Internal ASI04 (memory poisoning) = official ASI06 Memory & Context Poisoning; official ASI04 = Agentic Supply Chain. Internal ASI09 (cascading failure) = official ASI08. Official ASI05 (Unexpected Code Execution) has no dedicated internal category. The field carries TWO namespaces: MCP01-MCP10 are official OWASP MCP Top 10 codes; ASI01-ASI10 are AIShield internal. Most Agentic scanners emit MCP0x - of the 11 Agentic modules only scanner/memory_integrity_scan.py emits an internal ASI code (ASI04) - so filtering reports on ASI0x misses the majority of Agentic findings. Machine-readable crosswalk: the internal_to_owasp_agentic key returned by compliance_summary(). Source of truth: scanner/compliance.py (INTERNAL_ASI_TO_OWASP, OWASP_AGENTIC_2026, OWASP_AGENTIC_THREATS T1-T17)."
}
