# AIShield Security Benchmark v1

> 本表由 `python scripts/benchmark.py --markdown` 生成，请勿手工编辑。
> 语料固定、扫描确定性、零网络 —— 任何人跑同一份代码应得到同样的数字。

## 总分

| 指标 | 值 |
|---|---|
| 规则数 | MCP 235 / Skill 241 |
| 正样本（应检出） | 50 |
| 检出（critical/high） | 45 |
| **召回率** | **90.0%** |
| 负样本（应不报） | 45 |
| 误报（critical/high） | 0 |
| **误报率** | **0.0%** |
| 规则覆盖（任一 finding） | 50 / 50 |
| **规则覆盖率** | **100.0%** |

> **两套口径，各自内部一致**：主口径是 `serious_only`——只有 critical/high 才算检出、才算误报。这是运维真正会去处理的那一档，两个平面同口径，所以总分可以相加。副口径 `any_finding` 只用来算「规则覆盖」：规则层认不认得这个攻击意图。两者不相加、不混算。
> 
> 不为副口径配误报率：良性配置与文档上的 low/info 命中是信息性标注（例如「该配置使用运行时拉包」），不是误报。给它算 fp 会得到 40% 以上这种既不可操作也无法治理的数字。
> 
> 覆盖率高于召回率是正常且应该的：一条规则命中但只给了 medium，说明它认得这个意图却没给到可处理的严重度——那正是要盯的缺口，见「检出缺口」一节。

## 分平面

| 平面 | 检出线 | 正样本 | 检出 | 召回 | 覆盖 | 负样本 | 误报 |
|---|---|---|---|---|---|---|---|
| `instruction_plane` | `serious_only` | 28 | 24 | 85.7% | 100.0% | 35 | 0 |
| `config_plane` | `serious_only` | 22 | 21 | 95.5% | 100.0% | 10 | 0 |

## 参数化变体（配置面，按轴）

同一个攻击意图，换一种表面写法再测一遍。捡出「只在某一种写法上有效」的规则。

| 轴 | 样本 | 检出 | 召回 |
|---|---|---|---|
| `axis_credential=aws_access_key` | 4 | 4 | 100.0% |
| `axis_credential=generic_password` | 4 | 3 | 75.0% |
| `axis_credential=github_pat` | 4 | 4 | 100.0% |
| `axis_credential=openai_key` | 4 | 4 | 100.0% |
| `axis_launcher=docker_privileged` | 4 | 4 | 100.0% |
| `axis_launcher=npx_auto_install` | 4 | 4 | 100.0% |
| `axis_launcher=shell_wrapper` | 4 | 4 | 100.0% |
| `axis_launcher=uvx_auto_install` | 4 | 3 | 75.0% |
| `axis_risky_form=bash_c_official_pkg` | 1 | 1 | 100.0% |
| `axis_risky_form=npx_-y_official_pkg` | 1 | 1 | 100.0% |
| `axis_risky_form=remote_no_auth_header` | 1 | 1 | 100.0% |
| `axis_transport=remote_http_no_auth` | 1 | 1 | 100.0% |
| `axis_transport=remote_open_sse` | 1 | 1 | 100.0% |
| `axis_transport=remote_ws` | 1 | 1 | 100.0% |

### 报了但低于阈值（未达召回线，仍计入检出缺口）

| 样本 | 最高严重度 |
|---|---|
| `cfg-mal-uvx_auto_install-generic_password` | medium |

## 检出缺口（当前未到阈值，公开）

- `cfg-mal-uvx_auto_install-generic_password`
- `instruction_sample_#1`
- `instruction_sample_#13`
- `instruction_sample_#16`
- `instruction_sample_#23`


## 不变量

- 不发起任何网络请求
- 不执行被扫配置中的任何命令
- 输出确定性（同一份代码 → 同一组数字）

复现：`python scripts/benchmark.py --json`

