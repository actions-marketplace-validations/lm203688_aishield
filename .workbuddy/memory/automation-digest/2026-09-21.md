# 自动化运行摘要 — 2026-09-21（Beijing）

## AIShield Tech Radar（每日 02:00 CST，带 --publish）

- 命令：`scripts/tech_radar.py --sources all --days 7 --publish`
- 结果行：`[done] new=11 drafted=0 issues=0 errors=4`（exit 0，单次执行未重跑）
- 源健康：`[health] known-blocked sources (no action): reddit(8)`——**无 `[health] DEGRADED sources:` 行**（reddit 连续 8 次失败但已判 known-blocked，不计入降级升级）→ 不触发 🟡 升级。
- errors=4：Reddit 4 子版（LocalLLaMA/MCP/ClaudeAI/Anthropic）`HTTP -1`，网络出口结构性不可达，非脚本缺陷，远低于 >8 阈值 → 不告警。
- drafted=0：攻防侧分流生效（arxiv 侧多条目标 defense-side 未起草，如 ResumeShield 标 `prompt-injection _(defense-side, not drafted)_`）→ 无新威胁向量、无即时提醒、无跨项目升级。
- new=11：距上次运行约 24h（09-20 02:00 CST → 09-21 02:00 CST），`--days 7` 窗口高度重叠，seen_ids 1514→1525（+11）去重自然偏低，非故障。
- `--publish` 全 OK：report→57bfc2b9 / index.md→1c3ad861 / state→c4f6dfb6（本机出口 api.github.com 本次连通，无复发 sha 故障）。
- 状态：runs **54**、seen_ids **1525**、last_run `2026-09-20T18:03:02Z`。
- 升级判定：三项均不触发（无新攻击类别 / errors≤8 / 无连续 2 天 DEGRADED）→ 仅写本地 digest，不发独立提醒、不写 OPEN_ISSUES.md、未改代码、未手动 promote、未建 issue。

## AIShield 每日安全守夜（08:30 CST）

- 日期：2026-09-21
- 检查项结论：
  - ① 对外发布物自检（self_scan --json）：**PASS** — `blocking_unsuppressed=0`、`stale_allowlist_entries=[]`、`ok=true`、no_execute/no_network=true；12 源 45 发现 / 21 抑制（阻断级全自指误报，逐源空），无漏报、允许清单无失效、无需新增条目。
  - ② 线上存活：**PASS** — 远端 health.json `updated=2026-09-20T20:56:04Z`（age≈3.60h，FRESH）、healthy=true/score=4/4/consecutive_failures=0；aishield.tools 首页 200 / 28821B；/api/v1/health 200 / v4.3.0 / 235 规则 / commit eed3df14 / deployed_at 2026-09-20T08:38:30Z（≈15.9h，FRESH）。main HEAD cf9c147 领先 → 日级漂移（Spine 11:17 CST 追平，非异常）。
  - ③ 测试门禁（tests/run_all.py）：**PASS** — 1244 通过 / 0 失败 / 26 跳过（ALL TESTS PASSED；较 09-20 基准 +45 用例，失败恒 0）。日志 `FAIL: recall 0.9000 < 1.5000` 为故意门禁探针（test_benchmark.py），非真实失败。核心契约（良性零误报 + 恶意仍拦）成立。
- 关键异常清单：**无**。
- 升级判定：三项全绿，无关键异常 → 不触发即时对话提醒、不写 OPEN_ISSUES.md；仅本 digest + 报告 guard-2026-09-21.md。
