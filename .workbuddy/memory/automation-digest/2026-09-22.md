# 自动化运行摘要 — 2026-09-22（Beijing，02:00 CST = 09-21 18:03Z UTC）

## AIShield Tech Radar（每日 02:00 CST，带 --publish）

- 命令：`scripts/tech_radar.py --sources all --days 7 --publish`
- 结果行：`[done] new=50 drafted=0 issues=0 errors=4`（exit 0，单次执行未重跑）
- 源健康：`[health] known-blocked sources (no action): reddit(9)`——**无 `[health] DEGRADED sources:` 行**（reddit 连续 9 次失败但已判 known-blocked，不计入降级升级）→ 不触发 🟡 升级。
- errors=4：Reddit 4 子版（LocalLLaMA/MCP/ClaudeAI/Anthropic）`HTTP -1`，网络出口结构性不可达，非脚本缺陷，远低于 >8 阈值 → 不告警。
- drafted=0：攻防侧分流生效（arxiv 侧多条目标 defense-side 未起草，如 CASCADE/HE-Guardrail 标 `jailbreak _(defense-side, not drafted)_`、ResumeShield 标 `prompt-injection _(defense-side, not drafted)_`）→ 无新威胁向量、无即时提醒、无跨项目升级。
- new=50：距上次运行约 24h（09-21 02:00 CST → 09-22 02:00 CST），`--days 7` 窗口高度重叠，seen_ids 1525→1575（+50）去重自然正常，非故障。
- `--publish` 全 OK：report→55c00cfb / index.md→ce309ee1 / state→897c3ce6（本机出口 api.github.com 本次连通，无复发 sha 故障）。
- 状态：runs **55**、seen_ids **1575**、last_run `2026-09-21T18:03:04Z`。
- 升级判定：三项均不触发（无新攻击类别 / errors≤8 / 无连续 2 天 DEGRADED）→ 仅写本地 digest，不发独立提醒、不写 OPEN_ISSUES.md、未改代码、未手动 promote、未建 issue。

## AIShield 每日安全守夜（08:30 CST）

- 日期：2026-09-22
- ① 对外发布物自检：**PASS** — `blocking_unsuppressed=0`、`stale_allowlist_entries=[]`、`ok=true`、no_execute/no_network=true；49 发现 / 22 抑制，12 源逐源阻断级发现均空（全为自指误报按 `(source,file,type)` 精确消噪），无漏报、允许清单无失效、无需新增条目。
- ② 线上存活：**PASS** — 远端 health.json `updated=2026-09-21T22:04:12Z`（age≈2.46h，FRESH）、healthy=True/score=4/4/consecutive_failures=None；aishield.tools 首页 HTTP=200 / 28818B；/api/v1/health HTTP=200 / v4.3.0 / 235 规则（static 208+generated 8+radar 19）/ commit b7d9c18b / deployed_at 2026-09-21T08:58:48Z（≈23.5h，FRESH）；main HEAD 691e5a09 领先 → 日级漂移（Spine 11:17 CST 追平，非异常）。
- ③ 本地测试：**PASS** — 1244 passed / 0 failed / 26 skipped（ALL TESTS PASSED）；`recall 0.9000<1.5000` 为故意门禁探针，Failed=0 不升级；核心契约（良性零误报 + 恶意仍拦）成立。
- 关键异常清单：无。
- 处置：常规日，仅写本地 digest + 本报告（eco/reports/guard-2026-09-22.md），不触发即时对话提醒、不跨项目升级；改动文件经 `_push_batch.py` 单提交推送并 Contents API 复验。
