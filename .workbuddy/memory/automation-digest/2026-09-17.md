# AIShield 每日安全守夜 digest — 2026-09-17

## Tech Radar（06:24 CST / 09-15 22:26Z，--publish）
- 结果 `[done] new=37 drafted=0 issues=0 errors=4`，exit 0，单次执行未重跑。
- 源产出：arxiv 40 / reddit 4 / user-platforms 1 / **github-trending 0** / hn 0 / standards 0。
  - github-trending 0 且耗时 31.1s：非 4xx，疑当日窗口内 trending API 无命中或临时限流；脚本无独立健康门，未触发降级。
  - hn 0：与 09-15 一致，过滤窗口天然低命中，非故障。
- errors=4 连续同源同因（Reddit 4 子版 HTTP -1），远低于 >8 → 不告警。
- **`[health] known-blocked sources (no action): reddit(5)`**——`consecutive_failures` 累加至 5，脚本已改判 known-blocked 不再计入 DEGRADED；输出中**无 `[health] DEGRADED sources:` 行** → 未触发降级升级。
- **drafted=0** → 无规则候选、无新威胁向量 → 常规仅入 digest，无即时提醒、无跨项目升级。
- new=37：距上次日志记录（09-15T18:02Z）约 32.4h，本次 seen_ids 累计 1336→**1434（+98）**。差值 +61 属 00:39Z 早一次未记日志的运行累积（本地 `--days 7` 去重窗口内）。本次单次运行仅贡献 +37。
- **`--publish` 部分失败（第 2 次同一故障模式，跨日复发）**：report 成功（→f794ee7b），`docs/intel/index.md` 与 `data/state/tech_radar.json` 均报 `"sha" wasn't supplied`，且 `sha lookup status=-2`（HTTP 层未连通，非 404）。
  - 09-15 00:39Z 首次出现、09-15 18:02Z 一次成功看似"已修"，本次是第 2 次复发 → 说明 09-15 18:02Z 的成功只是恰好 api.github.com 通，非脚本已修。
  - 根因判读（不猜测代码）：`status=-2` 是 libcurl 连接层错误，指向 api.github.com 从本机出口仍间歇不可达；publish 分支"更新既有文件"必须先 GET sha，网络不通则拿不到。
  - 影响：远端 index.md 无今日条目、远端 tech_radar.json 停留在上次快照；M8 元监控读远端会误判。本地已正确更新。
  - 未改脚本（任务指令：失败直接报告，不改代码）。建议：publish 分支对 status=-2 增加退避重试，或改为一次 `_push_batch.py` 批推。
- 状态：`data/state/tech_radar.json` runs **51**、seen_ids **1434**、last_run `2026-09-16T22:26:22`（脚本内本地时区串；≈09-16T14:26Z）。
- 升级判定：新攻击类别=无、DEGRADED 连续 2 天=无、errors>8=无 → 未写 OPEN_ISSUES.md，仅写本地 digest，未发独立即时提醒。未手动 promote、未建 issue、未改代码。
- 备注：同一份执行记录亦写入 2026-09-16.md（脚本 UTC 时间落在 09-15 22:26Z、CST 09-16 06:24）；跨日两文件并存，供按 CST/UTC 分别检索。

## 每日安全守夜（08:30 CST / 00:30Z）
- 检查结论：**三项 PASS**（常规日，无关键异常、无跨项目升级）。
- ① 对外发布物自检 `self_scan --json`：`ok=true`、`blocking_unsuppressed=0`、`stale_allowlist_entries=[]`、`no_execute/no_network=true`；44 发现 / 20 抑制（全自指误报，11 源 blocking 均空：mcp-server/src 6/5 / guardrail-harness 12/9 / github-marketplace 4/3 / action_entrypoint 3/3 等），无未登记阻断、无漏报、允许清单无失效条目。
- ② 线上存活：aishield.tools 200 / 28509B；`/api/v1/health` v4.3.0 / 238 规则 / commit `f65e4986` / deployed_at `2026-09-16T08:39:15Z`（~16h，FRESH）；远端 health.json（Contents API）`updated` `2026-09-16T21:26:55Z`（~3.1h，FRESH，4/4 healthy）；main HEAD `f794ee7b` 日级漂移（Spine 11:17 CST 追平，非异常）。
- ③ 本地测试 `tests/run_all.py` **1028/0/0/44 全绿**（与 09-16 基准一致），核心契约（良性零误报 + 恶意仍拦）成立。
- 关键异常清单：无。
- 产出 `eco/reports/guard-2026-09-17.md` + 本 digest 追加；单提交推送（_push_batch.py）并 Contents API 复验。
- 工具链备注：本机沙箱下 curl `-o` 会丢弃写出文件且 body 显 0B（GitHub API 同日 0B 已证伪），改用 shell 重定向后抓取正常；属抓取特性，不影响存活判定。
