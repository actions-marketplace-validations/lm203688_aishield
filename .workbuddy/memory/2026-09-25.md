# 2026-09-25 工作日志

## NetMind Arena 出赛 — 第七轮（01:33 GMT+8）
- `jev_player.py tick --max-joins 3` → joined **0**（scanned 20 / unsupported-type=17 / not-worth-joining=3，EXIT=0）。
- 3 类受支持竞赛（echo / point-of-no-return / fog-maze）在板面但全 `status=upcoming`，
  被 `join_gate()` 的 `live`-only 规则拦下 —— 第 3 轮结构性死锁复现；
  `min=max=4` / `currentParticipants=2`（第 3 轮为 1）/ 时间字段全 null / creator=平台侧。
- 战力零变化：credits=200 / games_played=7 / games_won=0 / earnings=0 / is_verified=false。
  历史 7 场记录确认已登记 Weekly Credit League - Week 39（live），Echo 曾得 240 分。
- Product Arena：STATUS=pending（xp_VhvfZN00Sk），未重提。
- Jev 通路证伪性检查：`selftest` OK，4/4 真实调用成功 ⇒ 无动作是 gate 时相问题，非故障。
- 新踩坑：`/api/v1/agents/me/competitions?status=all` 数据键是 `competitions`+`total`（非 `data`），
  与 `/api/competitions?joinable=true&compact=true` 的 `data` 不同，探测器需分别取键。
- 产出：`_scratch/arena_round7_report.md`、`_scratch/arena_probe_r7.py`（复用探测脚本）。

## NetMind Arena 出赛 — 第八轮（03:34 GMT+8）
- `tick --max-joins 3` → scanned 20 / joined **0** / `unsupported-type=20`，EXIT=0。
  **4 类受支持竞赛全缺席**（第 7 轮短暂回归的 echo/PoNR/fog-maze 再度下线）→ 回到第 5/6 轮形态；
  类型分布与第 5/6 轮逐项一致。fee=0 共 5 个（bounty/poll-prediction×1/stock-prediction×2/weekly-arena），
  均不在 CATALOG → 范围纪律内跳过。
- 战力：credits=200 / games_played=7 / games_won=0 / earnings=0 / is_verified=false —— **连续第 7 轮零变化**；
  `last_active_at` 刷新为 2026-09-24T19:34:57Z。
- 参赛记录 7 条：5 场 ended（Echo score=240 / 其余 0；rank 2~4）+ Bounty(live) + Weekly Credit League Week 39(live)。
- Product Arena：STATUS=pending（xp_VhvfZN00Sk，publishedAt=None），公开目录 21 产品、aishield present=False，未重提。
- Jev 通路：本轮 tick 侧零调用；`selftest` **OK**（4/4 真实调用 conf .40/.42/.79/1.0）⇒ 非故障。
- **新踩坑（已修探测器）**：`/api/v1/agents/me/competitions?status=all` 的行是**扁平前缀键**
  （`competition_id` / `competition_name` / `competition_status` / `participant_status` / `rank` / `score` 等），
  无嵌套、无裸 `status`/`type` → 按 `r["status"]` 或 `r["competition"]["type"]` 取值会**全解析成 None**，
  伪装成"记录缺失"而实际有 7 条。已把 `_scratch/arena_probe_r7.py` 的取键逻辑改为兼容 `competition_` 前缀。
- 产出：`_scratch/arena_round8_report.md`。

## NetMind Arena 出赛 — 第九轮（05:37 GMT+8）
- `tick --max-joins 3` → scanned 20 / joined **0** / `unsupported-type=20`，EXIT=0。
  **4 类受支持竞赛全缺席（第 4 次同形态）**；类型分布与第 5/6/8 轮**逐项一致**
  （poll-prediction 4 / derby 4 / stock-prediction 2 / ftg 2 / negotiation 2 / weekly-arena 1 /
  ftg-tournament 1 / bounty 1 / undercover 1 / mun 1 / tank-battle 1）⇒ 排期自 01:33 起冻结逾 4 小时。
  fee=0 共 5 个，均不在 CATALOG → 范围纪律内跳过。未重试。
- 战力：credits=200 / games_played=7 / games_won=0 / earnings=0 / is_verified=false —— **连续第 8 轮零变化**；
  `last_active_at` 刷新为 2026-09-24T21:37:38Z。
- Product Arena：STATUS=pending（xp_VhvfZN00Sk，publishedAt=None），公开目录 21 产品、aishield present=False，未重提。
- Jev 通路：tick 侧零调用；`selftest` **OK**（4/4 真实调用 conf .38/.44/.84/1.0）⇒ 非故障。
- 距 Weekly Credit League 截止 2026-09-27T23:59Z 约 2 天 18 小时，仍在窗口内。
- 产出：`_scratch/arena_round9_report.md`（探测器 `arena_probe_r7.py` 复用无改动）。
- 流程优化备注：连续多轮同形态 ⇒ 后续轮次可只记增量差异，不必逐轮重复全量报告。

## 07:39 Arena 第 10 轮出赛（自动化）
- `tick --max-joins 3` → scanned 20 / joined **0** / `unsupported-type=20`，EXIT=0。与第 5/6/8/9 轮同形态，
  类型分布逐项一致 ⇒ 排期自 01:33 起冻结逾 6 小时。fee=0 共 5 个，均不在 CATALOG，未扩支持、未重试。
- 战力：credits=200 / games_played=7 / games_won=0 / earnings=0 / is_verified=false —— **连续第 9 轮零变化**；
  `last_active_at` → 2026-09-24T23:39:13Z。
- Product Arena：STATUS=pending（xp_VhvfZN00Sk，publishedAt=None），公开目录 21 产品、present=False，未重提。
- Jev：tick 侧零调用；`selftest` **OK**（conf .39/.48/.81/1.0）⇒ 两侧鉴权正常，非故障。
- 距 League 截止 2026-09-27T23:59Z 约 2 天 16 小时，仍在窗口内。
- 产出：`_scratch/arena_round10_report.md`（已 present）。

## 09:48 Arena 第 11 轮出赛（自动化）
- `tick --max-joins 3` → scanned 20 / joined **0** / `unsupported-type=20`，EXIT=0。
  **4 类受支持竞赛全缺席（第 6 次同形态）**，未扩支持、未重试。
- **排期解冻（新）**：第 5/6/8/9/10 轮冻结逾 8 小时的类型分布本轮变动 ——
  poll-prediction 4→3、ftg 2→1、tank-battle 1→0 下线、paper-portfolio 0→2 与 eden 0→1 回归
  ⇒ 平台排期约每数小时轮换一次，但**受支持 4 类是否回归不可预测**。
- 战力：credits=200 / games_played=7 / games_won=0 / earnings=0 / is_verified=false —— **连续第 10 轮零变化**；
  `last_active_at` → 2026-09-25T01:48:10Z。
- Product Arena：STATUS=pending（xp_VhvfZN00Sk，publishedAt=None，reviewNote=None），
  公开目录 21 产品、present=False，未重提。
- Jev：tick 侧零调用；`selftest` **OK**（conf .35/.51/.82/1.0）⇒ 两侧鉴权正常，非故障。
- 距 League 截止 2026-09-27T23:59Z 约 2 天 22 小时，仍在窗口内。
- 产出：`_scratch/arena_round11_report.md`（已 present）。

## 10:07 竞赛窗口月度扫描（自动化 6d3832c9）
- 扫 7 个目标（Foresight / NetMind / OWASP RT 2027 / AIxCC@NeurIPS / HK AIC 2nd / 火山 Skill 2nd / GitLab AI 2027）。
- 交付：`docs/competitions/README.md` 追加 `### 2026-09-25 automated scan`，删除「Prep work」里 AIxCC 假阳性行，AIxCC 入「Not pursuing」注明原因。
- 推送：`bc579c8d` `chore(competitions): 2026-09-25 monitoring scan` via `scripts/gh_push.py`。
- 触发项：无（Foresight 距截止 36 天；无 ≥$10K 新机会 30 天窗口内；无取消/延期）。
- **重要校正**：AIxCC 是 DARPA 项目（aicyberchallenge.com），2025 DEFCON 决赛已结、现过渡期；**不存在「AIxCC @ NeurIPS 2026」**。
- OWASP RT Landscape 已改滚动提交 / 季度发布，「2027-03 est」表述作废——**触发「现在即可提交」**。
- HK AIC 2nd 与 火山 Skill 2nd、GitLab AI 2027：均无官方新周期公告。
- 下轮（10-25）Foresight 距截止 6 天 → 应触发 URGENT 提醒（automation_update 一次一次性任务）。



## v4.8.3：NVIDIA 接入 + Agent 基础设施开源扫描管道 + 双形态联调（11:00-11:40 GMT+8）

### 用户战略澄清（本轮核心输入）
- 明确「**只接国外** Muse / Grok Bot 等个人 Agent，不接国内」；之前误建的 `connectors/coze/` 已删，Coze 仅保留 platform_registry 参考条目。
- 新方向三点：① 对 laya / nasiko / agent-desktop 等 **agent 基础设施开源项目**做「平台开源扫描 → 封装 → 二次研发」多维度多层级构建；② 国外个人 Agent 通过 **MCP 形态**或**开发者身份形态**双路对接；③ **NVIDIA 开发者平台**作接入底座。
- 用户拍板：三块**一起完成**。

### 落地
1. **NVIDIA 真实接入** `connectors/nvidia/`（NGC Catalog + NIM 推理 + NeMo 编排，NGC API Key 形态，非 OAuth）；13 测试全绿。
2. **Agent 基础设施开源扫描管道** `connectors/agent_infra/scan_pipeline.py`：三层交付物（report / mcp_adapter_skeleton / secondary_rd_checklist），三态输入（repo_url / local_path / 内存 files），复用 scanner.engine 底层函数，零依赖离线；15 测试全绿。
3. **双形态联调** `tests/test_dual_form_integration.py`：13 测试全绿。开发者身份（OAuth/PAT/API Key）与 MCP 桥（platform 参数化）同一入口，verdict 一致，PAI DID 与 HMAC 行动链跨平台共用。
4. **API**：`/api/v1/agent-infra/{targets,scan,scan-portfolio}` + nvidia self-check/register/run；server.py 两个 prefix 路由到 `connectors_api`。
5. **MCP**：manifest 62 → **66 工具**（+aishield_agent_infra_targets / aishield_agent_infra_scan），`nvidia-dev` 加入三个 connector 工具枚举；`npx tsc` 编译通过；**MCP stdio 实测 `tools/list` 返回 66 工具**。

### 修掉的真实 bug（非测试断言问题）
- 🔴 `connectors/nvidia/nvidia.py` 的 `pa.record_action(agent_instance_id=..., target=...)` 参数名全错（应为 `instance_id=`），被 `try/except: pass` **静默吞掉** → NVIDIA 行动**从未上 HMAC 链**。这正是"假绿"典型：行动链声称可用，实际从未记录。已改为与 muse/grok 同构的调用。
- `connectors/nvidia/__init__.py` 只有 docstring、未导出任何符号 → `from connectors.nvidia import self_check` 直接失败。已补 `__all__` 导出。
- `api/connectors_api.py` 用了 `NVIDIA_PLATFORM_ID` 但未导入（NameError）；agent-infra 扫描路由被 `if not plat: 400` 提前拦截；未知 `/api/v1/agent-infra/*` 返回 400 而非 404；3 个未使用 import。全部修复。
- `connectors/dispatcher.py` 缺按平台 kwargs 白名单 → 统一 MCP schema 的 `model`/`agent_instance_id` 传给不支持的连接器会 TypeError。已加 `_REGISTER_KWARGS` / `_PREFLIGHT_KWARGS` / `_RUN_KWARGS` 过滤（含 `http_post_mock` 离线测试接缝）。
- `eco/agent_card.py`：`test_agent_card_sign_and_verify` 失败的根因是 pub key 文件声明 ed25519、私钥已降级 hmac-sha256 → `publish_pubkey()` 重发布后恢复。

### 已知三平台 API 不一致（未统一，测试已兼容）
`run_agent_action` 返回值：Muse 的 `verdict` 是**字符串**，Grok/NVIDIA 是 **dict**。后续可统一。

### 三平台注册签名差异（记一下，省得下次踩）
- `muse.register_muse_agent(user_id, agent_name, platform_agent_id, client_id, tokens=None, ...)` — **client_id 必填**，无 agent_instance_id 参数（自动生成）
- `grok.register_grok_agent(user_id, agent_name, platform_agent_id, auth='pat'|'oauth', credentials=None, ...)` — 无 agent_instance_id 参数
- `nvidia.register_nvidia_agent(user_id, agent_name, agent_instance_id, platform_agent_id=None, api_key=None)` — agent_instance_id 必填
- `pa.record_action(user_id, action, payload=None, instance_id=None, ticket_id=None, verdict='allow', actor_did=None, note='')` — 是 **`instance_id`** 不是 `agent_instance_id`
- `pa.get_user_state(user_id)` 键：`actions_recorded`（不是 total_actions）、`agent_instances`、`active_tickets`、`chain_valid`、`did`、`budget_summary`、`connector_reviews`、`open_disputes`、`tickets_total` 等
- `pa.create_personal_did()` 返回 dict 直接含 `did`，**无 `success` 键**

### 门禁
- `sync_version.py --set 4.8.3 --sync`：25 文件一致 ✅
- `rule_count_gate.py`：47 声明位一致 ✅（235/262 未动）
- 套件：connectors 52 / nvidia 13 / agent_infra_scan 15 / dual_form 13 / platform_registry 31 / mcp_contract 18 — 全绿
