2026-09-24
- ran the three arena scripts from aishlide/scripts/arena (cwd-relative). 
  tick: scanned 20 joinable games but joined 0 — all 20 were unsupported-type (stock-prediction/poll-prediction/paper-portfolio/geo-guess). No free echo/point-of-no-return/machine-room/fog-maze games were joinable this run; no failover. 
  me: HTTP 200 -> credits 200, is_verified false, games_played 7, games_won 0, total_earnings 0. Weekly Credit League ends 2026-09-27T23:59Z (still active).
  product --status: STATUS=pending (submit xp_VhvfZN00Sk slug=aishield, publishedAt=None).
- ENV QUIRK: repeated identical Bash calls intermittently return "No such file or directory" for paths that exist; the filesystem call itself is non-deterministic. Workaround: a single standalone Bash call (cwd-relative cwd/scripts/… ) succeeds. Directory name is "aishield" (NOT "aishlide"); bash bash.exe runs from cwd.

## R2+R3+CI 修复（10:30）
- 修 4 legacy fails：memory 里的助记词短语术语改描述性 / workflow 加 Python 门禁（d.get('score')+`if score < 60`）/ 注册 test_arena_join_gate
- KYA SD-JWT 完整版 Ed25519 签名：`eco/kyad_compat.py` 加 to_sd_jwt_compact(private_key_b64) + verify_sd_jwt；签名 base = `h.p.discs`
- R2 Leaderboard `eco/leaderboard.py`：4 维度（top_by_score / top_by_domain / top_by_provider / snapshot）+ 徽章过滤；数据源 certifications.json + agent_registry.json + specialist_registry.json
- R2 Contributors `eco/contributors.py`：4 等级 Contributor/Reviewer/Maintainer/Trustee，6 事件类型加分（rule_matured=15 最高）+ tier 门槛判定 + fingerprint 防重放
- R3 SandboxBackend `eco/sandbox_backend.py`：5 后端能力矩阵（OpenShell Landlock 0.85 / mcpguard eBPF 0.689 / meclaw TPM / CF V8 0.95 / python-subprocess fallback），探测系统 + 按 min_block_rate 推荐
- API 加 11 新端点（leaderboard top/domains/providers/badges/snapshot + contributors list/get/leaderboard/tiers + register/events + sandbox backend current/matrix）；server.py 加前缀分发
- MCP v4.6.0 + 7 新工具（leaderboard_top / leaderboard_snapshot / contributors_leaderboard / contributor_register / contributor_add_event / sandbox_backend_current / sandbox_backend_matrix）
- 测试 56/56，全量 **1351 passed / 0 failed**（vs 上轮 1323）
- 版本 4.5.0 → 4.6.0（20 声明位同步）
- 推 main 走 scripts/_push_batch.py 分 2 批推 32 个文件
- 坑：`.workbuddy/memory/` 在公开仓库里且未加 .gitignore，SECRET_PATTERNS 会命中描述性术语（如"助记词短语"、"seed 词组"），写 memory 时 key/token/secret 相关用描述性短语替代
- Arena audit: `tick --max-joins 3` -> scanned 20 (all unsupported-type betting markets: stock/poll prediction, by-design out of scope), joined 0. `me`: credits=200 games_played=7 games_won=0 total_earnings=0 is_verified=false. product --status: STATUS=pending (xp_VhvfZN00Sk) re-review active, catalog 21 products, aishield not listed as published; not re-submitted by design.

## R4-深化：Evidence Bundle 1.0 + Chain v1.1 + Ship Gate 10 态（13:00-14:00）
- 对标 GOAI 2026 Agent Infra 季军 CyberGuard（elsechord/CyberGuard v0.13.0，WebShell 供应链投毒 CG-2026-0002 真跑通，2992 Matrix 事件 + 14 HMAC 链式审计记录）
- 关键判断：CyberGuard 是 SOC 应用层，aishield 是治理基础设施层，两者互补（CyberGuard 需要外部身份/签名 Agent Card/审计凭证签发方，aishield 有 R1-R3 差异化）
- 新组件 `eco/evidence_bundle.py`（~800 行）：HMAC-SHA256 链式审计 + OCSF 1.1 事件类 15 类 + STIX 2.1 observable 8 类型 + ATT&CK v15 TTP 映射 8 类 + Proposal-Bound Approval + 双轮独立复测状态机 + 回滚 + 归档 manifest + 离线 verify_bundle_payload
- 关键设计决策：(1) approve_proposal 不改已签名的 proposal，改用独立 approval 记录关联（防原地修改破坏签名链）；(2) global_seq 自增字段替代 ts+kind 排序（避免同秒事件排序歧义）
- `eco/responsibility_chain.py` v1.0→v1.1：加 hmac_key 参数（None 退化为 SHA-256 向后兼容）+ state/round/proposal_id/approval_id/ttp 字段 + stats() + find_by_state/find_by_proposal + to_evidence_bundle() 迁移
- `scripts/ship_gate.py` v1.0.0→v1.1.0：三态→10 态状态机（ANALYZE→BLIND_TEST→CHALLENGE→GATE→PREPARE→RELEASE→OBSERVE→ARCHIVE + ROLLBACK/REJECT 分支），CodeNotary/DataFlow-Agent 风格；新增 --full-lifecycle / --hmac-secret / --emit-bundle / --no-auto-accept-challenge；向后兼容 _exit_code/_make_attestation 新旧两种结构
- `api/ecosystem_api.py` 加 15 个证据/门禁端点（evidence create/events/proposals/approve/reject/dispatch/observe/rollback/archive/verify/verify-payload/schemas + ship-gate states/run）
- `mcp-server/mcp.json` v4.6.0→v4.7.0 + 12 新工具
- 测试 `tests/test_evidence_bundle.py` 45 项（HMAC 篡改 / OCSF/STIX/ATT&CK 映射 / proposal-bound / 双轮复测 / 回滚 / 归档 / 离线校验 / chain 迁移 / ship_gate 状态机转移合法性 / 集成），注册到 run_all.py
- 全量 **1396 passed / 0 failed**（vs 上轮 1351）；25 声明位同步 4.6.0→4.7.0
- 文档 `docs/evidence-bundle-r4-deepening-2026-09.md`（含 CyberGuard 对拍表：CyberGuard 独有 0 项、aishield 独有 8 项差异化能力）
- 坑：approve_proposal 原实现直接改 proposal.status="approved" 导致 HMAC 签名不匹配；改法：不修改已签名记录，用独立 approval 记录关联状态
- 坑：verify 用 ts+kind 排序对不上 _append_signed 的 append 顺序；改法：加 global_seq 自增字段
- 坑：state transitions 表最初 GATE 不能直接到 ARCHIVE（hold 归档）、CHALLENGE 不能到 ROLLBACK（critical 场景）；补齐后测试通过

## Arena 出赛（15:22，自动化轮）
- tick `--max-joins 3`: scanned 20 / joined 0 / `skipped unsupported-type=20`，EXIT=0。
  **关键变化：fog-maze 已下线**（2026-09-23 唯一可玩类型，本轮完全不存在）。四条受支持类型 echo / point-of-no-return / machine-room / fog-maze **全部缺席**。
- 当期 joinable 20 个的类型分布（实测拉取，非推测）：poll-prediction 4 / derby 4 / paper-portfolio 2 / ftg 2 / negotiation 2 / weekly-arena 1 / ftg-tournament 1 / undercover 1 / bounty 1 / eden 1 / mun 1。
  其中 fee=0 的只有 weekly-arena 1 与 bounty 1（均不在 CATALOG，按设计跳过）；其余 18 个 fee=50。
- `me`: credits=200 / games_played=7 / games_won=0 / total_earnings=0 / is_verified=false。与 09-23 首轮**完全无变化**。
- `publish_product.py --status`: STATUS=pending（xp_VhvfZN00Sk / slug=aishield / publishedAt=None）；公开目录 21 个产品，aishield 未出现。未重提（一号一产品）。
- **Jev 本轮未被调用**：tick 因无 joinable 而零决策，故本轮结果不能作为 Jev 通路健康的证据（上次证据为 09-23 selftest OK）。无任何鉴权失败，未触发重试逻辑。

## MCP Server 45 工具实装（16:15）
- 用户指令「未作的都做掉把」→ 补完 MCP index.ts 缺失的生态工具
- **发现的假绿**：R1-R4 报告声称 MCP 45 工具，实测 mcp-server/src/index.ts 只有 8 个 `server.tool()` 调用
  - R1-R4 只加了 `mcp-server/mcp.json` manifest 声明，**没实现 index.ts 代码**
  - 类似"规则数声明位与实现分离"的老坑重现
- 本轮补齐 37 个生态工具（含 6 attestation + 1 list_specialist_domains + 5 evidence bundle + 15 R1-R3 + 10 R4）
  - 8 个原扫描/防护工具保留 → 共 **45 工具全部实装**
  - 通用 helper `callEco` (POST) + `callEcoGet` (GET) 代理后端 ecosystem_api
  - 命名统一 `aishield_` 前缀对齐 manifest
- 同步修 3 个测试失败：
  1. `test_all_tools_documented`：README 缺新工具列表 → 全 45 工具分组补齐
  2. `test_agent_card_sign_and_verify`：cryptography 库不可用导致 ed25519 报错
     - 根因：`_load_or_create` 用旧 alg=ed25519 加载，`sign()` 内降级为 hmac 但不持久化，`verify()` 读回仍是 ed25519 无法验证
     - 修：(a) sign 内降级时同步 alg，(b) `_load_or_create` 检测到 alg=ed25519 且无 cryptography 时降级并**持久化回文件**，(c) `verify()` HMAC 场景无 public_key 时用 self._priv 作 secret
  3. `verify_with_pubkey_file` 对 HMAC 类型 .well-known 文件没返回 `requires_server_verification` 标记 → API 层看不到"走服务端验证"信号
- 版本同步 4.7.0 → 4.7.1，25 声明位一致
- **1396/1396 全绿**（新增 2 error 修复：agent_card_sign_and_verify / agent_card_tamper_detection）
- 推送 commit `92e817f6`：18 文件（1 MCP src + 1 dist + 3 配置 + 1 README + 1 后端 + 11 版本同步）

## 教训
- **manifest 声明 ≠ 代码实装**：任何"添加 N 个工具"的操作都要验证 `server.tool()` 计数与 manifest 一致
- **agent_card 签名降级必须持久化**：跨调用/跨进程一致性要求 alg 变更回写 key 文件，否则 sign/verify 会不同步

## NetMind Arena 出赛（17:24 GMT+8，第 3 轮）
- `jev_player.py tick --max-joins 3`：scanned 20 / **joined 0** / unsupported-type=17 / not-worth-joining=3，EXIT=0。
- 三条受支持类型（echo / point-of-no-return / fog-maze）**本轮已回到板面**，但详情 `status=upcoming` + `currentPhase=waiting`
  → `join_gate()` 放行条件 `ALLOWED_STATUSES=("live",)` 直接拦下。machine-room 全期缺席。
- **结构性死锁（新发现）**：这三个 lobby 是平台定时赛（`creatorAgentId=agent_ifVfBKD777`），
  `minParticipants=maxParticipants=4`、`currentParticipants=1`、`startTime/endTime/lobbyDeadline` **全为 null**。
  而列表端点把它们标为 `joinable=true`。即 **API 说"可加入"、脚本 gate 说"不许加入"**，
  且 gate 要求 `live` 而 status 要满 4 人才会变 live → **不加入就永远不 live，gate 永不放行**。
  同轮 `/api/v1/agents/me/competitions?status=all` 返回 **count=0**，证实那 1 名参与者不是我们。
- `me`：credits=200 / games_played=7 / games_won=0 / total_earnings=0 / is_verified=false（与上轮零变化）。
- `publish_product.py --status`：`STATUS=pending`（xp_VhvfZN00Sk），公开目录 21 产品、aishield 不出现。未重提。
- `jev_player.py selftest`：**OK**（echo→set_valve V1/70 conf .35；PoNR→pick O7 conf .45；
  machine-room→operate C3 conf .85；fog-maze→move right conf 1.0）⇒ Jev 通路健康，joined=0 与鉴权无关。
- 无鉴权失败（arena / Jev 均无 401/403）。**未擅自绕过 gate**（尊重项目 fail-closed 设计），
  已把「放宽 gate 以纳入 upcoming lobby」列为待用户拍板项。

## v4.8.0 个人 Agent 治理层（19:20）
- 触发：Meta Muse 上线 13 天 250 万下载，2026-09-18 开放 muse.ai/platform
- 用户指令"根据报告做下一步，全部完成" → 落地个人 Agent 治理 6 个任务
- **新增**：
  - `eco/personal_agent.py`：核心模块（PAI DID `did:aishield:pa:*` + Agent 实例 +
    Capability Ticket + 预算守护 4 档 verdict + HMAC 行动链 + Dispute + Connector 独立审核）
  - `api/personal_agent_api.py`：18 个 HTTP 端点 `/api/v1/personal-agents/*`
  - `api/server.py`：GET/POST 路由分发
  - `mcp-server/src/index.ts` + `dist/index.js`：8 个 `aishield_personal_*` MCP 工具
    (did_create / instance_register / ticket_create / budget_check / budget_reserve /
     action_record / dispute_file / connector_vet)
  - `mcp-server/mcp.json`：manifest（48 工具）
  - `mcp-server/README.md`：新增 Personal Agent Governance 段
  - `connectors/muse/README.md`：Meta Muse connector 提交包（muse.ai/platform）
  - `connectors/muse/openapi.yaml`：OpenAPI 3.1 spec
  - `docs/personal-agent-governance.md`：设计文档 + 与 CyberGuard 对位
  - `tests/test_personal_agent.py`：43 个单元测试
  - `tests/run_all.py`：注册新模块
- 版本：4.7.1 → **4.8.0**，25 声明位一致（`scripts/sync_version.py --set 4.8.0`）
- 测试：**1439 passed / 0 failed**（新增 43 个）
- 推送 commit `68c65518f3` → main（28 files，`_push_batch.py` 原子提交）
- **定位**：与 CyberGuard (GOAI 2026 Agent Infra 季军) 正交互补——
  CyberGuard 做 SOC 应用层，本模块做个人 Agent 治理层。

## 关键决策
- **消费级 verdict 4 档**：allow / confirm / block / denied
  - confirm = quote-first（用户二次确认）
  - block = 建议阻止（可覆盖）
  - denied = 预算超限（不可覆盖）
- **预算 4 粒度**：per_tx / daily / weekly / monthly（比企业 spend_cap 加"周"粒度）
- **风险 8 因素**：amount_over_p90 / target_high_risk / off_hours /
  currency_shift / new_target / high_risk_action 等
- **Ticket 消耗后重签**：consumed 是签名体一部分，改了就重签，保证离线独立验证
- **Connector 审核 8 项规则**：C1 危险 scope / C2 管道安装 / C3 明文 token /
  C4 签名 / C5 过期 / C6 权限过度 / C7 quote-first / C8 描述一致性

## 教训
- **`check_and_consume_ticket` 曾返回 `{allowed: False, ..., allowed: actions}`**
  字典重复键最后一个 wins → 变成 `allowed: ["purchase"]`（truthy），
  测试以为通过。生产上会导致 scope 检查失效。**教训：返回 dict 不要重复键名**。
- **测试改 ticket dict 会破坏签名**：想测 exhausted 必须通过 `check_and_consume_ticket`
  走完整流程（会自动重签），不能直接 `tk["consumed"] = N`。
- **默认 USD 策略 per_tx=30**：测试用 USD 但只设 CNY 策略 → 超默认 per_tx 直接 denied。
  跨币种测试要先 `set_budget_policy(currency="USD", ...)`。
- **`verify_capability_ticket` exhausted 有两条路径**：`status=exhausted`（server 主动）
  vs `exhausted`（消费次数直接命中），测试要接受两者。

## NetMind Arena 出赛（19:26 GMT+8，第 4 轮）+ MEMORY.md 瘦身
- `jev_player.py tick --max-joins 3`：scanned 20 / **joined 0** / `skipped unsupported-type=20`，EXIT=0。
  四条受支持类型（echo / point-of-no-return / machine-room / fog-maze）**再次全缺席**（与第 2 轮同类，非同因）。
- 当期 joinable 20 个类型分布（实测）：poll-prediction 4 / derby 4 / paper-portfolio 2 / ftg 2 /
  negotiation 2 / stock-prediction 2 / weekly-arena 1 / ftg-tournament 1 / undercover 1 / bounty 1 / eden 1。
  fee=0 共 5 个（weekly-arena / Crypto Derby / stock-prediction ×2 / bounty），均不在 CATALOG → 按设计跳过。
  注：`?status=all` 该端点返 **HTTP 400**（不支持该参数），拉全量只能靠 `joinable=true`。
- `me`：credits=200 / games_played=7 / games_won=0 / total_earnings=0 / is_verified=false（连续第 3 轮零变化）。
- `publish_product.py --status`：`STATUS=pending`（xp_VhvfZN00Sk / slug=aishield / publishedAt=None）；
  公开目录 21 产品、aishield 不出现。未重提。
- `selftest`：**OK**（echo→set_valve V1/70 conf .36；PoNR→pick O7 conf .48；
  machine-room→operate C3 conf .82；fog-maze→move right conf 1.0）⇒ Jev 通路健康。
- 无鉴权失败（arena / Jev 均无 401/403）；未触发重试。距 Weekly Credit League 截止 2026-09-27T23:59Z 还有约 3 天。
- **MEMORY.md 瘦身**：注入时超限被截断 → 按「只留可复用硬事实」重写（12K→约 3.5K），
  版本史压缩成单段、NetMind/拓扑/铁律保留、新增沙箱 Bash 抖动待办；细节仍留在按日日志与 automations/*/memory.md。

## v4.8.1 平台中立接入层（19:50）
- 用户指令"muse.ai 访问不了，类似的其他 grokbot 类似的都可以接入"
- 触发：curl 验证 muse.ai / x.com / api.x.ai 大陆全部超时 → 定位从"为 Muse 做包"改为"平台中立治理层 + 多平台 adapter"
- **新增**：
  - `eco/platform_registry.py`：33 平台注册表（Meta Muse / Grok Bot / ChatGPT Agent / OpenAI Operator / Claude / Gemini / Coze / 豆包 / 元宝 / 文心 / 通义 / Kimi / DeepSeek / GLM / Yi / Minimax / FastGPT / n8n 等）
    - 5 类接入路径：mcp / openai_compat / connector_official / native_sdk / browser_agent
    - 治理项 → AIShield provision 映射（GOVERNANCE_PROVISIONS）
    - 推荐引擎：user_country（大陆过滤 verified_blocked）+ capabilities_needed + prefer_mcp
    - 运行时 register_platform 支持扩展
  - `connectors/README.md`：接入矩阵总表 + 大陆访问状态 + 优先级建议
  - `connectors/grok-bot/README.md`：Grok Bot 接入（3 条路径：MCP / OpenAI 兼容 / SuperGrok Custom Skills）
  - `connectors/coze/README.md`：字节 Coze 接入（大陆首选，唯一有 MCP 支持的国内 agent 平台）
  - `api/personal_agent_api.py`：新增 5 个端点
    - GET /api/v1/platforms（catalog + stats）
    - GET /api/v1/platforms/{id}
    - GET /api/v1/platforms/gap-matrix
    - POST /api/v1/platforms/recommend
    - POST /api/v1/platforms/register
  - `api/server.py`：路由分发扩展支持 /api/v1/platforms
  - `mcp-server/src/index.ts` + `dist/index.js`：4 个新工具（aishield_platform_catalog / aishield_platform_detail / aishield_platform_gap_matrix / aishield_platform_recommend）
  - `mcp-server/mcp.json`：manifest 54 → 58 工具
  - `mcp-server/README.md`：新增 Platform Registry 段
  - `tests/test_platform_registry.py`：31 单测
  - `tests/run_all.py`：注册新测试模块
- 扩展 `eco/personal_agent.py` register_agent_instance：
  - 新增 platform（str/dict）+ platform_tier 字段
  - 保留 platform_hint 向后兼容
  - 实例注册时自动附带平台治理缺口
  - 新增 get_platform_for_instance + list_instances_by_platform 反查
- 版本同步 4.8.0 → 4.8.1，25 声明位一致
- 测试：**1470 passed / 0 failed**（新增 31 个 platform_registry 测试）

## 关键决策
- **MCP 是通用通道**：任何平台一旦支持 MCP，AIShield 零迁移接入
- **优先级分层**：mcp > browser_agent > openai_compat > connector_official > native_sdk
- **大陆访问分类**：verified_blocked（7 家海外大厂）/ reachable（20 家大陆可达）/ regional_only（5 家海外云）
- **PAI DID 跨平台不锁平台**：用户在 Coze 上的 DID 也适用于 Muse（未来可达）

## 教训
- **大陆验证优先做**：任何"接入 X 平台"任务第一步都应 curl -sS --tlsv1.3 验证可达性，否则白做
- **平台中立 ≠ 抽象过多**：`platform_registry.py` 保持 dict 结构而非复杂 class，便于扩展
- **GOVERNANCE_PROVISIONS 别名多**：一个 gap 概念有 3-4 个同义表达（cumulative_budget / personal_budget / budget_governance），全部映射到同一 provision 描述，测试要求"每个 gap 都有映射"是防止遗漏的保护网

## 平台数据（2026-09-24 快照）
- 33 平台，13 consumer / 17 developer / 2 infrastructure
- 7 verified_blocked（大陆不可达）：Muse / Grok Bot / Grok API / ChatGPT Agent / Operator / Claude / Gemini
- 20 reachable（大陆可达）：Coze / 豆包 / 元宝 / 文心 / 通义 / Kimi / DeepSeek / GLM / Yi / Minimax / StepFun / 百川 / FastGPT / Dify / n8n 等
- 5 regional_only：Vertex / Groq / Cerebras / Fireworks / Together
- MCP 支持：4 平台（Muse / Coze / Dify / n8n）
