# 2026-09-20 工作日志

## 闭环融合与修复（用户指令：尽量减少无效和空转的任务，闭环任务需要修复的直接修复掉）

### 盘点
- 20 个 GitHub Actions workflow + 5 条 AIShield 本地自动化，全部解析触发方式/定时/作业/权限。
- 结论：**无真正空转的闭环**，5 条自动化职责互不重叠（雷达/自检/竞品/分发/周报），无需融合。

### 🟥 已修复的真红灯：geo-indexnow 9/9 全红
- **根因**：`geo-indexnow-submit.yml` workflow 级 `permissions: contents: read`，
  心跳步骤 `git_push_safe.sh` 每次 push 403 → 重试 5 次 → `exit 1` → 整条 job 失败。
  核心 key-check + submit 在全部 9 次运行中**都成功了**——只是末步装饰性心跳拖成红灯。
- **修法（双层）**：① `permissions: contents: read → contents: write`；
  ② 心跳 push 加 `|| true`（best-effort 状态回写，不该盖过核心红绿灯）。
- 横向核查 20 个 workflow 的 push/权限匹配：只有 geo-indexnow 一个真问题
  （`ci.yml` 的 push 在 job 级已提权；`publish-npm` 的 git push 只在注释里）。
- `validate_workflows.py` 20/20 通过。推送 `49132475`。

### 🟡 已更新的过期基线（2 条自动化 prompt）
- 每日安全守夜：测试基准 670 → **1199 通过 / 26 跳过**。
- 分发缺口巡检：版本基线 4.2.2/227 规则/6 工具 → **4.3.0/235 规则/7 工具**（含 aishield_digest）。
- 过期基线会导致「测试失败数 > 0」或「版本漂移」误判。

### 关键确认（记录在案，非故障）
- `self_scan.py` **不在任何 CI workflow 里**（unified-security-scan 扫的是仓库自身态势，
  不是台账外发布物）→ 本地守夜是台账外自检的唯一执行者。by design（需读本地全树 + allowlist）。
- `tech_radar.py` 不被任何 workflow 调用 → by design（需 WebSearch/agent 推理，CI 跑不了）。
- spine 第 1 步注释叫「雷达数据采集」但实际接 `data-scan-flywheel`（批量扫描），
  与真正雷达 `tech_radar.py` 不是一回事。已记录不改注释（改需动 spine，风险 > 收益）。

### 新增文档
- `docs/intel/2026-09-20-closed-loop-topology.md`：闭环拓扑诊断，含全量 workflow/自动化清单、
  空转判据、已修红灯、过期基线、不需修的记录在案项。

### 遗留（需用户本人）
- 吊销曾硬编码进 public 仓库历史的旧 CF token。
- aishield.tools CF Pages Retry（3 个 cfut_ token 权限不足，无法 API 触发）。

## server-card 第 7 工具修复 + 部署时序竞态（10:30）
- `api/static/.well-known/mcp/server-card.json` 只有 6 个工具，缺 `aishield_digest`；
  且描述里 skill 规则数写 241（实际 244）。本地/远端 git/线上**三方一致地错**——不是部署问题，
  是代码遗漏。修后推 `f9890861`，18/18 MCP 契约测试通过。
- 🟥 **部署时序竞态**：dispatch（01:07:36Z）比 `f9890861` 提交（01:08:01Z）**早 25 秒**，
  dispatch 时 main HEAD 还是 `57f048b0`（部署成功但部署的是旧 sha）→ 线上 server-card 仍 6 工具。
  **push 与 workflow_dispatch 不是原子操作**：推送完成前触发的 dispatch 会部署到推送前一刻的 HEAD。
  → 修法：push 完成后**先查 main HEAD 再 dispatch**，dispatch 后核对 run 的 `head_sha` 是否含目标 commit。
  已重新 dispatch（204，main @ `8192c9f1`）。

## 外部市场分析评估（10:44，用户转发四视角市场分析，要求评估哪些可作提升）
第一手核实结果（逐条对照，不照单全收）：
- ✅ 对：GitHub stars=2 / forks=0 / open_issues=7；ROADMAP 3 个月目标 stars 500 实际 2（上次评审 09-07）；
  `capability_gap.py` 存在（405 行，adopt 线分析）。
- ✅ **对且比它说的更糟**：规则数声明位混乱。`mcp-server/README.md` 里同时存在
  **113/201/208/235/241/62 六个数字**；`mcp-server/server.json` 写 skill 241（应 244）。
  它说「三个数字」低估了——实际 6 个。
- ❌ **错**：「没有任何基准数据支撑」——`docs/benchmark/v1.md` 存在（96.0% 召回 / 0.0% 误报 + 15 轴分解表），
  `scripts/benchmark.py` 存在。它主张「用可核验证据替代纸面 claim」，自己却没核验就下结论。
- ❌ **错**：「文档全是概念、缺 interactive demo」——`api/static/` 有 13 个 HTML 页面，
  含 `scan.html`(17.5KB) 在线扫描页、`attack-graph.html`、`scan_report.html`。
- ❌ 过期：skill 规则数 241（实际 244，server-card 已修对）。
- ⚠️ 部分对：`benchmarks/` 确实只有 6 个文件（malicious/index.js 仅 526B），demo 素材薄——
  但缺的是「预置恶意样本一键试」，不是从零做 demo。
- ❓ 无法独立验证：AgentAuditKit 271 规则 / 1100+ 测试 / 已上 Marketplace（外部竞品，无访问手段）；
  COMMUNITY.md 台账 `downloads: 1`（本地没匹配到该行）。
- 🟥 **它没提到的真问题**：`docs/` 目录在线上**完全不可达**——
  `/docs/benchmark/v1.md`、`/benchmark`、`/api/v1/benchmark`、`/docs/aishield-verification-harness.md` 全 404。
  即 benchmark 数据有、harness 文档有，**但对外通道没有**。这比它说的「没发布 benchmark」更精确。
- 结论：方向判断（先做可核验外部证据）大体对，但三条 P0/P1 的**前提事实错误**；
  「双市场稀释定位」「零依赖恰是边界市场」两条不建议采纳（后者与它自己在用户视角的论证自相矛盾）。
