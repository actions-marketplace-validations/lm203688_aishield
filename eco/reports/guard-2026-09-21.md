# AIShield 每日安全守夜 — 2026-09-21

> 执行时刻：2026-09-21 08:30 CST（自动化）
> 结论：**常规日（三项全绿，无关键异常）** — 仅写本地 digest，无跨项目升级。

## 三项检查结论

| # | 检查项 | 结论 |
|---|--------|------|
| ① | 对外发布物自检（self_scan --json） | ✅ PASS |
| ② | 线上存活（远端 health.json + aishield.tools 首页） | ✅ PASS |
| ③ | 测试门禁（tests/run_all.py） | ✅ PASS |

---

## ① 对外发布物自检

`python scripts/self_scan.py --json`：

- **`totals.blocking_unsuppressed = 0`** — 无未登记的阻断级发现（真实信号为 0）。
- **`stale_allowlist_entries = []`** — 允许清单无失效条目，无需清理。
- `ok = true`；不变量 `no_execute = true` / `no_network = true`（扫描器未执行任何被扫指令、未发起网络请求）。
- 覆盖 12 个源，共 **45 发现 / 21 抑制**；逐源 `blocking_unsuppressed` 均为空：
  - distribution/agensi/chinese-seo-compliance 97/1/0
  - distribution/claude-skill 96/2/0
  - distribution/gpt-store 95/2/0
  - distribution/huggingface 94/5/0
  - distribution/clawhub 94/2/0
  - mcp-server/src 76/6/5（抑制 5）
  - distribution/deepseek-harness 95/4/0
  - distribution/guardrail-harness 75/13/10（抑制 10）
  - distribution/github-marketplace 87/4/3（抑制 3）
  - distribution/listings 95/3/0
  - action_entrypoint.py 88/3/3（抑制 3）
- 阻断级发现逐条复核：全部为扫描器自指误报（检测词汇 / 演示载荷 / 只读 preflight），已由 `self_reference_allowlist.json` 按 (source,file,type) 精确消噪并保留理由。**无需新增 allowlist 条目。**
- 脚本自身正常退出（exit 0），无报错。

## ② 线上存活

- **远端 health.json**（GitHub Contents API，ref=main，非本地副本）：
  - `updated = 2026-09-20T20:56:04.719532+00:00`，age ≈ **3.60h** → **FRESH**
  - `current.healthy = true`，`level = healthy`，`score = 4/4`，`consecutive_failures = 0`
- **aishield.tools 首页**：HTTP **200**，28821 B。
- **/api/v1/health**：HTTP **200**，`version = 4.3.0`，`rules_count = 235`（208 静态 + 8 生成 + 19 雷达），`commit = eed3df14`，`deployed_at = 2026-09-20T08:38:30Z`（≈ 15.9h，FRESH）。
- 部署新鲜度：线上 commit `eed3df14` 落后 main HEAD `cf9c147` → **日级漂移（正常）**，Spine（11:17 CST）下次运行将追平，非异常。

## ③ 测试门禁

`python tests/run_all.py`：

- **Passed = 1244 / Failed = 0 / Errors = 0 / Skipped = 26** → **ALL TESTS PASSED**。
- 核心契约（良性零误报 + 恶意仍拦）成立：benchmark 副口径 50/50 覆盖 100.0%、serious 口径 recall 45/50 = 90.0%、fp 0.0%。
- 注：日志中 `FAIL: recall 0.9000 < 1.5000` 来自 `test_benchmark.py` 的**故意门禁探针**（`--fail-under-recall 1.5` 验证门禁会红），非真实失败；本次 `Failed = 0`。
- 较 09-20 基准 1199 → 1244（+45 用例，参数化/新增用例增长），失败数恒 0。

---

## 处置

- 常规日，无关键异常：**不**触发即时对话提醒、**不**触发跨项目升级（OPEN_ISSUES）。
- 已生成本报告 `eco/reports/guard-2026-09-21.md` 与本地 digest `2026-09-21.md`，单提交推送并 Contents API 复验。
- 临时探针文件（health_remote.json / aishield_home.html / aishield_health.json / repo_info.json / main_ref.json）已清理，不入库。
