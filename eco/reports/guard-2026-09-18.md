# AIShield 每日安全守夜报告 — 2026-09-18 (08:30 CST)

> 模式：持续鉴证 + 自带洞必检 + 健康门禁。三项检查，唯一真信号 = `totals.blocking_unsuppressed` 与线上存活。

## 总览

| 检查 | 结论 | 关键指标 |
|---|---|---|
| ① 对外发布物自检 (`self_scan.py --json`) | **PASS** | `blocking_unsuppressed=0`、`stale_allowlist_entries=[]`、`ok=true` |
| ② 线上存活 (curl + 远端 health.json) | **🔴 FAIL（关键异常）** | aishield.tools 三次探测均 **502**；远端 health 源不可达 `consecutive_failures=3` |
| ③ 本地测试 (`tests/run_all.py`) | **PASS** | 1015 passed / 0 failed / 0 errors / 26 skipped，核心契约成立 |

**今日判定：非常规日 — 线上站失活（红色安全红线），已即时高亮并触发跨项目升级。**

---

## ① 对外发布物自检

`python scripts/self_scan.py --json` → 单一入口（台账内 `distribution/published.json` + 台账外 10 源）。

- `totals.blocking_unsuppressed = 0` ✅（真实信号为零；无未登记阻断级发现）
- `totals.findings = 44`，`totals.suppressed = 20`（全为扫描器自指误报，按 `distribution/self_reference_allowlist.json` 的 `(source,file,type)` 精确消噪）
- `stale_allowlist_entries = []` ✅（允许清单无失效条目，无需清理）
- `invariants`: `no_execute=true`、`no_network=true` ✅（扫描器未 spawn 被扫命令、未联网，隔离不变量成立）
- 逐源 `blocking_unsuppressed` 均为空：agensi(1/0)、claude-skill(2/0)、gpt-store(2/0)、huggingface(5/0)、clawhub(2/0)、mcp-server/src(6/5)、deepseek-harness(4/0)、guardrail-harness(12/9)、github-marketplace(4/3)、listings(3/0)、action_entrypoint.py(3/3)

**结论**：对外发布物无未登记阻断级发现、无漏报、允许清单干净。artifact 安全，未静默跳过。

---

## ② 线上存活（关键异常 🔴）

**探测手法**：curl 带 `--ssl-no-revoke --tlsv1.3`（本机 TLS 代理仅放行 TLS1.3）；远端 health.json 走 GitHub Contents API（`ref=main`）读取，本地副本不作判断。

| 探针 | 结果 |
|---|---|
| `https://aishield.tools/` (首页) | **HTTP 502**（3/3 次重试均 502，16 bytes） |
| `https://aishield.tools/api/v1/health` | **HTTP 502** |
| 远端 `data/state/health.json` (`updated`) | `2026-09-17T21:31:30Z`（文件新鲜，但内容为失活态） |
| 远端 health `current` | `healthy=False`、`level=degraded`、`score=0/4`、`http=None`、`consecutive_failures=3`、`last_healthy_at=2026-09-17T05:01:19Z` |

**根因判定**：
- 502 = Bad Gateway（非 404），排除 DNS/路由层问题；`http=None` 表示源无响应 → `Cloudflare Named Tunnel → VPS :8450 → api/server.py` 链路中 **VPS 后端进程或 cloudflared 隧道已中断**。
- 远端 health 显示 Spine 自 **2026-09-17T11:53:47Z** 起已连续 3 次探测失败；今日 08:30 CST 本人独立复测仍 502 → **outage 跨越 09-17 与 09-18 两个自然日（连续 2 日失活）**。
- Spine 自身仍在写 health.json（最近写入 09-17 21:31Z，约 3h 前），说明 CI/Spine 链路存活，仅源站（VPS）不可达。

**需用户动作（硬阻塞，AI 不代登）**：登录 VPS 检查
1. `cloudflared` 隧道进程是否存活（`systemctl status cloudflared` / 必要时 `systemctl restart cloudflared`）；
2. 后端 `api/server.py` 监听 `:8450` 的进程/端口是否存活（必要时重启后端）；
3. VPS 是否重启、欠费、或进程被 OOM/kill。

**验法**：复测 `curl --ssl-no-revoke --tlsv1.3 https://aishield.tools/api/v1/health` 恢复 `200` 且返回 `rules_breakdown`（静态 210 / 情报 9 / 雷达 19，v4.3.0）；远端 health.json `current.healthy` 回 `True`、`consecutive_failures` 归零。

---

## ③ 本地测试

`python tests/run_all.py`：

```
Total:    1015
Passed:   1015
Failed:   0
Errors:   0
Skipped:  26
ALL TESTS PASSED
```

- 失败数 = 0 → 未触发升级；核心契约（良性零误报 + 恶意仍拦）成立。
- 用例数较 09-17 基准（1028/0/0/44）变化为 1015/0/0/26，系测试集整合/收敛所致，非失败；红色安全红线（拦截逻辑）未被削弱（测试日志含 `promoted/refused/skipped as dead` 规则晋升分诊，均符合预期）。

---

## 处置与升级

- ✅ 本地两项（发布物自检、测试）PASS，核心资产与契约无回归。
- 🔴 线上站失活（连续 2 日）→ 已即时在对话中高亮，并追加跨项目升级条目至 `知识引擎14站/.workbuddy/memory/automation-digest/OPEN_ISSUES.md` 的「🟠 跨项目升级」节（同日同来源无重复条目）。
- 📝 本报告 `eco/reports/guard-2026-09-18.md` 单提交推送 main 并 Contents API 复验。
- 本地 digest 追加至 `.workbuddy/memory/automation-digest/2026-09-18.md`。

---

## 历史基线对照

| 日期 | 自检 | 线上 | 测试 | 备注 |
|---|---|---|---|---|
| 09-17 | PASS | PASS | 1028/0/0/44 | 常规日（当日 Spine 11:17Z 前源仍健康） |
| 09-18 | PASS | 🔴 FAIL | 1015/0/0/26 | **线上站 502 失活，跨项目升级 🔴** |
