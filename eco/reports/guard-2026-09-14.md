# AIShield 每日安全守夜报告 — 2026-09-14

> 运行时刻：2026-09-14 08:30 (CST, UTC+8) · 守夜 agent 自动化
> 结论：**三项全部 PASS（常规日）** — 无关键异常、无跨项目升级、无即时高亮。

## 1. 对外发布物自检（单一入口 `scripts/self_scan.py --json`）

| 指标 | 值 | 判定 |
|---|---|---|
| `totals.blocking_unsuppressed` | **0** | ✅ 无未登记阻断级发现（真实信号为 0） |
| `totals.findings` | 44 | — |
| `totals.suppressed` | 20 | 全部为自指误报，按 (source,file,type) 精确消噪 |
| `stale_allowlist_entries` | `[]` | ✅ 允许清单无失效条目 |
| `ok` | true | ✅ |
| `invariants.no_execute` | true | ✅ 扫描器未 spawn 被扫配置里的命令 |
| `invariants.no_network` | true | ✅ 扫描全程无出站网络 |

**逐源消噪分布（与 09-13 基线一致，稳定）：**
- distribution 台账内（agensi / claude-skill / gpt-store / huggingface / clawhub / deepseek-harness / listings）：评分 94–97，findings 均已登记为安全（suppressed=0，blocking=0）
- `mcp-server/src`：68 分 / 6 发现 / 抑制 5（自指检测词汇）
- `distribution/guardrail-harness`：61 分 / 12 发现 / 抑制 9（演示载荷）
- `distribution/github-marketplace`：87 分 / 4 发现 / 抑制 3（演示/文档词汇）
- `action_entrypoint.py`：88 分 / 3 发现 / 抑制 3（只读 preflight 子进程）

**判定：** `blocking_unsuppressed=0` → 无真实高危漏报，无需新增 allowlist 条目。`stale_allowlist_entries=[]` → 允许清单无已失效条目、无需清理。**结论：对外产物安全、无自指误报被静默放过、无漏报。**

## 2. 线上存活（curl `--ssl-no-revoke --tlsv1.3`，远端读）

| 检查项 | 结果 | 判定 |
|---|---|---|
| 远端 `data/state/health.json` `updated`（GitHub Contents API, ref=main） | `2026-09-13T20:57:35Z`（约 3.5h 前） | ✅ FRESH（< 24h） |
| `aishield.tools/` 首页 HTTP | **200**（28.5 KB） | ✅ 存活 |
| `aishield.tools/api/v1/health` | v4.3.0 / **236 规则** / commit `566d8dc160bee4b9f501f837d21733e2353f1404` / deployed_at `2026-09-13T08:23:42Z`（约 16h 前） | ✅ 存活、部署新鲜 |
| main HEAD（GitHub API） | `7b9824602ec6244490e54cf6a817fb18d4b7021e` | 日级漂移（线上 commit 落后，Spine 11:17 CST 将追平，非异常） |

> 注：raw.githubusercontent.com 本次探测偶发 HTTP 000（TLS 代理抖动），改用 GitHub Contents API 复验成功，远端 `updated` 真实有效。
> 部署新鲜度：线上 commit ≠ main HEAD 属 Spine 日级漂移常态（cron `17 3 * * *` UTC = 11:17 CST），`deployed_at` 仍在 24h 内、进程代码新鲜，非「进程活着但代码陈旧」假绿。

## 3. 本地测试门禁（`tests/run_all.py`）

```
Total:    745
Passed:   745
Failed:   0
Errors:   0
Skipped:  12
============================================================
ALL TESTS PASSED
```

较 09-13 基准（733）持平并 +12（745 通过 / 0 失败 / 12 跳过）。核心契约（**良性零误报 + 恶意仍拦**）成立。

## 处置与推送
- 本报告 + digest 单提交推送（`scripts/_push_batch.py`），并以 Contents API 复验。
- 常规日：仅写本地 digest，不在对话单独提醒；无跨项目升级（OPEN_ISSUES.md 未追加）。

---
生成：AIShield 每日安全守夜 automation-1786840209961
