# AIShield 每日安全守夜报告 — 2026-09-22 08:30 CST

> 执行者：每日安全守夜 automation-1786840209961
> 模式：持续鉴证 + 自带洞必检 + 健康门禁（单一入口 `scripts/self_scan.py --json`）

## 总判：✅ 三项全绿（常规日，无关键异常，无跨项目升级）

| 检查项 | 结论 | 关键指标 |
|---|---|---|
| ① 对外发布物自检 | **PASS** | `blocking_unsuppressed=0`、`stale_allowlist_entries=[]`、`ok=true`、no_execute/no_network=true |
| ② 线上存活 | **PASS** | 远端 health.json healthy=True/score=4、aishield.tools 首页 HTTP 200、/api/v1/health 200 |
| ③ 本地测试 | **PASS** | 1244 passed / 0 failed / 26 skipped（ALL TESTS PASSED） |

---

## ① 对外发布物自检（`python scripts/self_scan.py --json`）

- **真信号**：`totals.blocking_unsuppressed = 0` —— 无未登记阻断级发现。
- **允许清单健康**：`stale_allowlist_entries = []` —— 无失效条目，无需清理。
- **不变量成立**：`invariants.no_execute=true`、`invariants.no_network=true`（扫描器自身零 spawn、零出网）。
- **总体**：49 发现 / 22 抑制（`ok=true`），12 源逐源 `blocking_unsuppressed` 均为空，全部命中自指误报（扫描器检测字样 / 演示载荷 / 只读 preflight），已按 `(source,file,type)` 精确消噪并保留理由。无漏报、无新增 allowlist 需求。
- 各源阻断级发现均为空（节选）：mcp-server/src（6 发现/5 抑制）、guardrail-harness（16/11）、github-marketplace（4/3）、action_entrypoint.py（3/3）、huggingface（6/0）、clawhub（2/0）等——逐源空，未以「全部自指误报」一句话带过。

## ② 线上存活（curl `--ssl-no-revoke --tlsv1.3`）

- **远端 health.json**（GitHub Contents API 读 ref=main，本地副本不算数）：
  - `updated = 2026-09-21T22:04:12Z`（age ≈ **2.46h，FRESH**）
  - `current.healthy = True`、`score = 4/4`、`consecutive_failures = None`
- **aishield.tools 首页**：`HTTP=200`、`SIZE=28818B`
- **/api/v1/health**：`HTTP=200`、version `4.3.0`、rules_breakdown `{static:208, generated:8, radar:19, total:235}`、commit `b7d9c18b...`、deployed_at `2026-09-21T08:58:48Z`（≈23.5h，<24h，FRESH）
- **部署新鲜度**：main HEAD `691e5a09` 领先线上 commit `b7d9c18b` —— **日级漂移（非异常）**，Spine 11:17 CST 将追平；deployed_at 仍在 24h 窗口内，未触发失活升级。

## ③ 本地测试（`python tests/run_all.py`）

- **1244 passed / 0 failed / 26 skipped → ALL TESTS PASSED**
- 核心契约（良性零误报 + 恶意仍拦）成立。
- 日志中 `FAIL: recall 0.9000 < 1.5000` 为**故意门禁探针**（benchmark 双口径校验，Failed=0 不升级），非真实失败。
- 较 09-21 基准（1244/0/0/26）持平，无回归、无新增失败。

---

## 处置

- 常规日：仅写本地 digest 与本报告，**不触发即时对话提醒、不跨项目升级**。
- 改动文件：`eco/reports/guard-2026-09-22.md`（新增）、`.workbuddy/memory/automation-digest/2026-09-22.md`（追加）。
- 改用 `_push_batch.py` 单提交推送并 Contents API 复验。
