# AIShield 每日安全守夜报告 — 2026-09-20

- 执行时间：2026-09-20 08:30 (CST) / 2026-09-19 23:30 (UTC)
- 执行身份：AIShield 每日安全守夜 automation（自带洞必检 + 健康门禁）
- 总体结论：**三项全部 PASS，常规日（无关键异常，仅写 digest，无跨项目升级）**

---

## 1. 对外发布物自检（`python scripts/self_scan.py --json`）

**结论：PASS**

| 指标 | 值 | 判定 |
|---|---|---|
| `totals.blocking_unsuppressed` | **0** | 真实信号：无未登记的阻断级发现 |
| `stale_allowlist_entries` | `[]` | 允许清单无失效条目，无需清理 |
| `ok` | `true` | 自检脚本正常完成 |
| `invariants.no_execute` | `true` | 扫描器未执行被扫配置中的任何命令（隔离不变量成立） |
| `invariants.no_network` | `true` | 扫描器未发起任何网络请求 |
| `totals.findings` / `suppressed` | 45 / 21 | 全部命中为自指误报，按 `(source,file,type)` 精确消噪 |

**逐源明细（blocking_unsuppressed 均为空，无真实高危、无漏报）：**

| 源 | 文件数 | 评分 | 发现 | 抑制 | blocking_unsuppressed |
|---|---|---|---|---|---|
| distribution/published.json (台账内) | — | — | — | — | [] |
| distribution/npm | 1 | 97 | 4 | 0 | [] |
| distribution/claude-skill | 1 | 99 | 1 | 0 | [] |
| distribution/gpt-store | 1 | 99 | 1 | 0 | [] |
| distribution/huggingface | 1 | 94 | 5 | 0 | [] |
| distribution/clawhub | 1 | 94 | 2 | 0 | [] |
| mcp-server/src | 1 | 76 | 6 | 5 | [] |
| distribution/deepseek-harness | 5 | 95 | 4 | 0 | [] |
| distribution/guardrail-harness | 4 | 61 | 13 | 10 | [] |
| distribution/github-marketplace | 3 | 87 | 4 | 3 | [] |
| distribution/listings | 1 | 95 | 3 | 0 | [] |
| action_entrypoint.py | 1 | 88 | 3 | 3 | [] |

**判定：** 全部 45 条发现均为扫描器自指误报（检测字样 / 演示载荷 / 只读 preflight），已按精确 allowlist 条目消噪并保留理由；`blocking_unsuppressed=0` 表示**无真实高危、无漏报、无需新增 allowlist 条目**。

---

## 2. 线上存活（GitHub Contents API + curl --ssl-no-revoke --tlsv1.3）

**结论：PASS**

- **远端 `data/state/health.json`**（Contents API, `ref=main`，本地副本不计数）：
  - `updated` = `2026-09-19T20:50:59Z`（相对执行时刻 age≈2.66h，**FRESH**）
  - `current.healthy` = `true`，`level` = `healthy`，`score` = **4/4**，`consecutive_failures` = **0**，`last_healthy_at` = `2026-09-19T20:50:59Z`
- **aishield.tools 首页**：`HTTP 200`，body 28821B
- **aishield.tools `/api/v1/health`**：`HTTP 200`，`version` = `4.3.0`，`rules_count` = **235**（静态 208 + 情报 8 + 雷达 19），`commit` = `c133bbedbdfeb51c6325290ab376fe7030997a1f`，`deployed_at` = `2026-09-19T14:03:08Z`（≈9.5h，<24h，**FRESH**）

> 注：`updated` 与首页/API 均指向近 24h 内，远端探针连续 0 次失败 → 线上站健康，昨日（09-18）的 502 失活已恢复且未复发。

---

## 3. 本地测试（`python tests/run_all.py`）

**结论：PASS（绿灯）**

```
Total:    1199
Passed:   1199
Failed:   0
Errors:   0
Skipped:  26
ALL TESTS PASSED
```

**核心契约校验：**
- 良性零误报（benchmark negatives 31，false pos 0，`fp rate 0.0%`）✅
- 恶意仍拦（benchmark positives 50，detected 48，`recall 96.0%`）✅（套件判定 ALL PASSED，Failed=0）

**观察（非升级项）：** 安全基准集 `recall = 96.0%`（50 个恶意样本检出 48，漏 2）。套件失败计数 = 0，未触发升级；`fp rate 0.0%` 维持，良性零误报契约成立。基准打印行 `FAIL: recall 0.9600 < 1.5000` 中阈值 `1.5` 恒大于 1，属脚本内部打印瑕疵，不影响套件失败计数，记录为信息性。

---

## 升级判定（D2 融合）

| 升级条件 | 是否满足 | 说明 |
|---|---|---|
| ① tests/run_all.py 转红（失败 > 0） | 否 | Failed=0 |
| ② 对外产物检出未登记阻断级发现（blocking_unsuppressed > 0） | 否 | =0 |
| ③ aishield.tools 首页或远端 health.json 失活（连续 2 天） | 否 | 今日全绿，昨日已恢复 |

→ **常规日，仅写本地 digest，无即时对话高亮、无跨项目升级。**
