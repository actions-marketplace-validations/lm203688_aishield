# AIShield 每日安全守夜报告 — 2026-09-13

> 运行时刻：2026-09-13 08:30 CST（定时自动化 automation-1786840209961）
> 结论：**三项 PASS，常规日（无关键异常、无跨项目升级）**

## 1. 对外发布物自检（`scripts/self_scan.py --json`）

| 指标 | 值 |
|---|---|
| 总发现 | 44 |
| 已抑制（自指误报，按 (source,file,type) 精确消噪） | 20 |
| **未登记阻断级发现 `totals.blocking_unsuppressed`** | **0** ✅ |
| 失效允许清单条目 `stale_allowlist_entries` | `[]` ✅ |
| 脚本状态 `ok` | `true` ✅ |
| 不变量 `no_execute` | `true` ✅ |
| 不变量 `no_network` | `true` ✅ |

分源抑制分布（仅列出有抑制的源）：

| source | files | findings | suppressed | blocking_unsuppressed |
|---|---|---|---|---|
| mcp-server/src | 1 | 6 | 5 | 0 |
| distribution/guardrail-harness | 4 | 12 | 9 | 0 |
| distribution/github-marketplace | 3 | 4 | 3 | 0 |
| action_entrypoint.py | 1 | 3 | 3 | 0 |
| 其余 7 源（agensi/claude-skill/gpt-store/huggingface/clawhub/deepseek-harness/listings） | — | 19 | 0 | 0 |

**判定**：全部 44 发现均为扫描器自指误报（index.ts 检测字样 / 演示载荷 / 只读 preflight），已由 `distribution/self_reference_allowlist.json` 精确消噪；无未登记阻断级发现、无漏报、允许清单无失效条目。台账内（distribution/published.json）+ 台账外（npm 源 / DSH / guardrail / github-marketplace / listings / skill 市场 / action_entrypoint.py）单一入口全覆盖。

## 2. 线上存活（curl --ssl-no-revoke --tlsv1.3）

| 探测项 | 结果 |
|---|---|
| 远端 `data/state/health.json`（GitHub Contents API, ref=main）`updated` | `2026-09-12T20:40:52Z`（约 3.8h 前，FRESH）✅ |
| aishield.tools 首页 | HTTP 200 / 28,509 字节 ✅ |
| `/api/v1/health` version | 4.3.0 |
| `/api/v1/health` rules_count | 236（static 210 + generated 9 + radar 17） |
| `/api/v1/health` commit | `81eb016a5fd4f1da0f898b66a6f811e6e41e8cd7` |
| `/api/v1/health` deployed_at | `2026-09-12T07:59:45Z`（约 16.5h 前，FRESH）✅ |

**部署新鲜度**：deployed_at 在 24h 内，进程活跃、代码非陈旧。线上 commit `81eb016a` 落后 main HEAD `a79713e62b53`（2026-09-12T20:41:16Z）属**日级漂移**——Spine cron 为 11:17 CST，今日尚未运行，非异常（与 09-12 同模式）。

## 3. 本地测试门禁（`tests/run_all.py`）

| 指标 | 值 |
|---|---|
| Total | 733 |
| Passed | 733 |
| Failed | 0 ✅ |
| Errors | 0 ✅ |
| Skipped | 12 |
| 核心契约 | 良性零误报 + 恶意仍拦 ✅ |

（较 09-12 的 699 增 34，新增用例全部通过，门禁仍全绿。）

## 结论

三项检查全部 PASS，无关键异常（测试未转红 / 对外产物无未登记阻断级发现 / 线上未失活）。按反馈集中化规则，**常规日仅写 digest，不发对话提醒、不触发跨项目升级**。

下次 Spine（约 11:17 CST 2026-09-13）将追平部署到最新 main HEAD。
